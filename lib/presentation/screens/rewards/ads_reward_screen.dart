import 'package:flutter/material.dart';

class AdsRewardScreen extends StatelessWidget {
  const AdsRewardScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('광고보상'),
      ),
      body: const Center(
        child: Text('광고보상 화면'),
      ),
    );
  }
}
